# 评价式反馈提示词 · 迭代记录

> **单一来源：`app/prompt.py`**（server.py 与 selftest.py 均从此导入）。
> 本文档只记录版本演进；具体提示词以 prompt.py 为准。
> `PsyCLIENT_反馈提示词_v2.md` 为历史文档，已被本记录取代。

## 版本演进

| 版本 | 改动 | 回归成绩 |
|---|---|---|
| v1 | 初版（判定规则 R1–R6 + rewrite_hint 禁令雏形） | flash 8/9（锚例集） |
| v2 | 拆分 is_reflection 与档次两步；候选清单规则；rewrite_hint 硬性禁令 | plus 9/9（锚例集）；**但真实日志人工同意率仅 42%** |
| v3 | missed 引用核验；两个工作示例（取自真实失败）；点评描述准确性（追问≠打断） | 16/18（锚例 9 + 真实 9） |
| v3.1 | 判定改为机械三步：先填 matched（对上的候选项），档次由 matched 非空与否直接决定 | 17/18 |
| **v3.2（现行）** | matched 准入收紧（松散对应不进 matched）+ **代码级 postprocess 强制 R3**（matched 非空 → hit，覆盖时记 verdict_overridden） | **18/18 = 100%，平均 3.5s** |

## 三条实证教训（改提示词前先读）

1. **干净锚例上的满分毫无保证力。** v2 在 9 条自写锚例上 9/9，遇到真实学员的长回应（情绪先行、覆盖多项要素）立即崩到 42%——真实作答必须进回归集。
2. **指令压不住的，用结构压；结构压不住的，用代码压。** "候选清单对上任一项即命中"写了三遍模型仍违反；改成"先机械填 matched、档次由 matched 决定"好转；最后仍有 ~5% 边界样本靠 postprocess 代码强制才归零。
3. **每个强制手段都会翻出新边界。** 强制 R3 后，泛化表达（"你挺不舒服"）被松散塞进 matched 而误判 hit——需同步收紧 matched 准入。改任何一处都必须全量回归。

## 回归测试

- 用例：18 条（9 锚例 + 9 真实学员回应，真实用例标注为 Claude 初标、待临床复核）
- 跑法：`python3 -c "import selftest as S; S.run_range(0,9,'glm-4-plus')"` 分两三段跑完 0–18
- 最近一次结果：`regression_latest.json`（18/18）
- **纪律：改 prompt.py 后必须全量重跑；新的真实失败样本应持续并入用例集**
