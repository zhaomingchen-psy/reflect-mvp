# REFLECT 上线指南

目标：同事打开一个网址就能用，不用装任何东西。

---

## 为什么不用 Vercel

Vercel（以及 Netlify Functions、Cloudflare Workers）是 **serverless**：每个请求可能落在不同的临时实例上，请求结束进程就销毁。我们的服务器有两样东西与之冲突：

1. **对话会话存在内存里**（`SESSIONS` 字典）。学员发第 2 轮时，接住请求的可能是另一个实例，会话就"不存在"了。
2. **日志写在本地文件里**（`logs/*.jsonl`）。serverless 的文件系统是临时的，写完就丢。

要上 Vercel 得把会话和日志都搬到外部数据库（Vercel KV / Postgres），是一次重写。MVP 阶段不值得。

**结论：用能跑常驻进程的平台。** 下面按推荐顺序给三个。

---

## 方案一：Render（推荐，免费，5 分钟）

免费版就能跑常驻进程，代码一行不用改。

### 步骤

**1. 把 `skills/mvp/` 推到 GitHub**

在 GitHub 新建一个**私有**仓库（比如 `reflect-mvp`），然后在终端：

```bash
cd ~/Desktop/Psyclient培训/skills/mvp
git init
git add .
git commit -m "REFLECT MVP"
git branch -M main
git remote add origin https://github.com/你的用户名/reflect-mvp.git
git push -u origin main
```

`.gitignore` 已经写好，`.env` 和 `logs/` 不会被上传——**密钥不进代码库**。

**2. 在 Render 建服务**

1. 打开 https://render.com 用 GitHub 账号登录
2. New → Web Service → 选刚才那个仓库
3. 关键设置（`render.yaml` 已备好，Render 会自动读取；若手填则填这些）：
   - Runtime: **Python 3**
   - Build Command: 留空（我们零依赖）
   - Start Command: `python3 app/server.py`
   - Instance Type: **Free**
4. Environment 里加两个变量：
   - `API_KEY` = 你的智谱 key
   - `ACCESS_CODE` = 自定一个口令（比如 `reflect2026`），同事进站要输一次
5. Create Web Service，等 2 分钟

**3. 拿到网址**

形如 `https://reflect-mvp.onrender.com`，把网址和口令发给同事即可。右上角可切中/英。

### 免费版的两个注意点

- **15 分钟没人访问会休眠**，下一个人打开要等约 30 秒冷启动。想避免就升 $7/月，或者用个免费的定时 ping 服务（如 cron-job.org 每 10 分钟访问一次 `/api/health`）保持唤醒。
- **重新部署会清空 `logs/`**。MVP 试用无所谓；等要留数据了，加一个 Render Disk（$1/月起），把环境变量 `LOG_DIR` 指向挂载目录即可——服务器已支持这个变量。

---

## 方案二：Cloudflare Tunnel（不想注册平台时用）

服务跑在你自己电脑上，Cloudflare 给一个公网网址。

```bash
brew install cloudflared
# 一个终端跑服务（注意加 ACCESS_CODE）
cd ~/Desktop/Psyclient培训/skills/mvp/app
ACCESS_CODE=reflect2026 HOST=127.0.0.1 python3 server.py 8000
# 另一个终端开隧道
cloudflared tunnel --url http://localhost:8000
```

会打印一个 `https://xxx.trycloudflare.com` 网址。**代价**：你的 Mac 得一直开着、终端不能关；国内访问 trycloudflare 域名偶尔不稳。

---

## 方案三：国内云服务器（正式跑 RCT 时用）

阿里云/腾讯云最低配（约 100 元/年）。优点：国内延迟低、不休眠、数据在自己手里。

```bash
# 登录服务器后
sudo apt update && sudo apt install -y python3 git
git clone https://github.com/你的用户名/reflect-mvp.git
cd reflect-mvp
# 用 systemd 常驻（简版：先用 nohup 试跑）
API_KEY=你的key ACCESS_CODE=你的口令 PORT=8000 nohup python3 app/server.py &
```

安全组放行 8000 端口，访问 `http://服务器IP:8000`。正式使用前建议：配 HTTPS（用 Caddy 一行搞定）、把 `LOG_DIR` 指向单独目录并定期备份。

> 用 IP 直连不需要备案；绑定域名做网站备案才需要。

---

## 上线前的检查清单

- [ ] `ACCESS_CODE` 已设置——否则任何人拿到网址都能用，会消耗你的 API 额度
- [ ] `API_KEY` 只在平台的环境变量里，**没有**出现在代码库中（`git log -p | grep -i api_key` 查一遍）
- [ ] 智谱账户余额够用（每次判定/生成约一次调用，一个学员完整练一轮约 40–60 次）
- [ ] 如果要留数据：`LOG_DIR` 指向持久磁盘，且定期下载备份
- [ ] 涉及真实学员数据前：走伦理审批，并在首页加知情同意说明

---

## 环境变量一览

| 变量 | 作用 | 默认 |
|---|---|---|
| `API_KEY` | 智谱 API 密钥（必填） | 无，缺则启动失败 |
| `BASE_URL` | API 地址 | 智谱官方地址 |
| `MODEL` | 判定与润色模型 | `glm-4.6` |
| `MODEL_GEN` | 来访者与小结模型 | 同 `MODEL` |
| `ACCESS_CODE` | 访问口令，非空则全站需要 | 空（不设防） |
| `PORT` | 监听端口 | 平台注入，或 8000 |
| `HOST` | 监听地址 | 有 `PORT` 时 `0.0.0.0`，否则 `127.0.0.1` |
| `LOG_DIR` | 日志目录 | `app/logs` |

本地开发不用管这些——`.env` 文件照旧生效，只监听本机。
